# Elementos Reutilizáveis

**Projeto:** gestao-contratos-conexao
**Total de Elementos Reutilizáveis:** 0 (nenhum encontrado no JSON)

Nenhum elemento reutilizável encontrado no JSON exportado.

> Nota: Elementos como popups (CARREGANDO, POPUP - INATIVO,
> POPUP - CONTA NÃO ENCONTRADA, REMOVER BARRA DE ROLAGEM) são
> reutilizados entre páginas, mas não estão definidos como
> `reusable_elements` formais no JSON.
