name: modulo-structure
description: Valida estrutura de diretórios de módulos
severity: error
rules:
  required_dirs:
    - components
    - pages
    - services
    - hooks
  required_files:
    - module.ts
    - permissions.ts
    - index.ts
    - types.ts
  optional_dirs:
    - lib
    - constants
